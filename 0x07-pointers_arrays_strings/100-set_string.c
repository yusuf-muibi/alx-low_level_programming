#include "main.h"
/**
* set_string - this is the function that set the value of the pointer to a char
* @s: the destination pointer
* @to: the character to be set
* Return: the value of the set pointer
*/
void set_string(char **s, char *to)
{
*s = to;
}
