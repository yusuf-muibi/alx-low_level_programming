#include "main.h"
/**
* _abs - this is the Function to compute absolute value
* @n: this is the parameter to check its abs
* Return: this returns the absolute value
*/
int _abs(int n)
{
return ((n < 0) ? -n : n);
}
