#include "main.h"
/**
* more_numbers - this prints numbers 0 through 14 ten times
* Return: returns void
*/
void more_numbers(void)
{
int t;
int u;
for (t = 0; t <= 9; t++)
{
for (u = 0; u <= 14; u++)
{
if (j > 9)
_putchar(j / 10 + '0');
_putchar(j % 10 + '0');
}
_putchar('\n');
}
}
