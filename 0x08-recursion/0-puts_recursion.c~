#include "main.h"
/**
* _puts_recursion - This is the function used to call the recursion function
* @s: This is the character to be printed
* Return: This returns the string of characters
*/
void _puts_recursion(char *s)
{
if (*s == '\0')
{
_putchar('\n');
return;
}
_putchar(*s)
_puts_recursion(s + 1);
}
