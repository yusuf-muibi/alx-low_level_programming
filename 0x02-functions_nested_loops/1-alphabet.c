#include "main.h"
/**
* print_alphabet - this is the main function used in the program
* Return: Always returns a 0 for a successful execution
*/
void print_alphabet(void);
{
char t;
for (t = 'a' ; t <= 'z' ; t++)
{
_putchar(t);
_putchar("\n");
}
}
