#include "main.h"
/**
* _islower - this is the Function to check if a character is lowercase 
* @c: this is the parameter to check its case
* Return: this returns a case indicator
*/
int _islower(int c)
{
return (c >= 'a' && c <= 'z');
}
