#include "main.h"
/**
* _isupper - this is the Function to check if a character is uppercase
* @c: this is the parameter to check its case
* Return: this returns a case indicator
*/
int _isupper(int c)
{
return (c >= 'A' && c <= 'Z');
}
