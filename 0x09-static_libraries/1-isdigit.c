#include "main.h"
/**
* _isdigit - this is the Function to check if a character is a digit
* @c: this is the parameter to check its value
* Return: this returns a value indicator
*/
int _isdigit(int c)
{
return (c >= '0' && c <= '9');
}
