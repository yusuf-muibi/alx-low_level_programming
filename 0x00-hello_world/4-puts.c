#include <stdio.h>

/**
 * main - main is the main function in use
 * Return: always 0
*/

int main(void)
{
char myput[] = "\"Programming is like building a multilingual puzzle";
puts(myput);
return (0);
}
