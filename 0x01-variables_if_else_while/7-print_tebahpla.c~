#include <stdio.h>
/**
 *main - This is the main function
 *Return: Always returns the zero value for a successful execution
 */
int main (void)
{
char t;
for (t = 'z' ; t >= 'a' ; t--)
putchar(t);
putchar('\n');
return (0);
}
