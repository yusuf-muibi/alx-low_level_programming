#include <stdio.h>

/**
* main - this is the main function in use
* Return: Always return 0 for a successful process
 */

int main(void)
{
char t;
for (t = 'a' ; t <= 'z' ; t++)
putchar(t);
putchar('\n')
return (0);
}
