#include <stdio.h>
/**
 * main - Main function of the program
 * Return: Always return zero for a successful execution
 */
int main(void)
{
int t;
for (t = 0; t < 10 ; t++)
putchar(t + '0');
putchar('\n');
return (0);
}
